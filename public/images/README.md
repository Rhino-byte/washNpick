# Images moved to Cloudinary

Site images are no longer stored in this folder. Upload and manage them at:

**`/admin/media`** (requires `ADMIN_UPLOAD_SECRET`)

Image slots use fixed Cloudinary public IDs defined in `lib/site-images.ts` under the `freshfold/` folder.
